# 1.1 Guest Login

## Front-end Components

- **WelcomeScreen Activity**: Main welcome screen that displays guest login option
  - btnWSkip Button: Button to skip authentication and login as guest
  - TextSwitcher: Displays app features during guest login flow
  - Animation components: Fade and scale animations for UI transitions

- **UI Layout (activity_welcome_screen.xml)**: Layout containing welcome image, feature text, and login buttons
  - ImageView: App logo display
  - Button components: Login and Skip buttons
  - TextSwitcher: Dynamic feature descriptions

## Back-end Components

- **Variables Class**: Stores guest user session data
  - userUID: Set to "guest" for guest sessions
  - userEmail: Set to "guest@speakforge.app"
  - userAccountType: Set to "guest"
  - userLanguage: Default "English"
  - userTranslator: Default "gemini"

- **SharedPreferences**: Persistent storage for guest user state
  - PREF_IS_GUEST_USER: Boolean flag indicating guest login status
  - Stores guest session across app restarts

## Plant UML Diagrams

### Class Diagram (MVC Model)

```plantuml
@startuml Guest Login Class Diagram

class "Model" as Model {
  - Classes: Variables.java, SharedPreferences
  - Methods: userUID, userEmail, userAccountType, 
    userLanguage, userTranslator, PREF_IS_GUEST_USER
}

class "View" as View {
  - Classes: WelcomeScreen.java, BaseAuthActivity.java
  - Layouts: activity_welcome_screen.xml
  - Elements: btnWLogin, btnWSkip, textSwitcher, welcomeImage, 
    textContent, buttonContainer, dotsIndicator, dot1, dot2, dot3, dot4
  - Methods: show(), dismiss(), showDialog()
}

class "Controller" as Controller {
  - Classes: WelcomeScreen.java
  - Methods: onCreate(), onClick(), setupAuthListener(),
    setGuestVariables(), saveToSharedPreferences()
}

Model --> Controller : provides data
Controller --> View : updates UI
View --> Controller : user input

@enduml
```

### Sequence Diagram

```plantuml
@startuml Guest Login Sequence Diagram

actor User
participant WelcomeScreen
participant Variables
participant SharedPreferences
participant MainActivity

User -> WelcomeScreen: Click "Skip" button
WelcomeScreen -> Variables: Set guest user data
Variables --> Variables: userUID = "guest"
Variables --> Variables: userEmail = "guest@speakforge.app"
Variables --> Variables: userAccountType = "guest"
WelcomeScreen -> SharedPreferences: Save PREF_IS_GUEST_USER = true
WelcomeScreen -> MainActivity: Navigate to main activity

@enduml
```

### Data Design Diagram

```plantuml
@startuml Guest Login Data Design Diagram

database "SharedPreferences" as Prefs {
  PREF_IS_GUEST_USER : boolean
}

class Variables {
  +userUID : String = "guest"
  +userEmail : String = "guest@speakforge.app"
  +userAccountType : String = "guest"
  +userLanguage : String = "English"
  +userTranslator : String = "gemini"
}

note right: Guest users do not create\nFirebase accounts

Prefs --> Variables : initializes
Variables --> Prefs : persists

@enduml
```